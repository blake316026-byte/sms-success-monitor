# v0.3.65

- macOS v0.3.65 build 72，Universal 2，支持 Apple Silicon 和 Intel。
- Windows 继续提供 v0.3.64；Android 继续提供已验证的 v0.3.61。
- 修复新增 `fpljsoft.com` 后台被误判为“仅浏览页面”，导致保存账号密码后不启动自动登录的问题。
- 只扩展已确认的 NPG 后台域名范围；其他未知网址仍保持仅浏览模式，不发送短信或财务查询。
- 保留 v0.3.64 的持久关键词高亮、当前页面优先启动登录等功能。

## 验证

- 完整项目测试、macOS Universal 构建、签名和打包自检通过。
- 旧 Mac 实测“后台账号 29”已自动登录并进入 `silver02` 主界面，登录后的 Token 已保存。
- Mini 和旧 Mac 均已安装并启动 macOS v0.3.65 build 72。
- GitHub 附件提供 SHA256 校验清单。

## macOS 首次打开

当前包使用内部临时签名，尚未使用 Apple Developer ID 签名和公证。从 GitHub 下载后若出现“Apple 无法验证”提示，请先将应用放入“应用程序”并尝试打开一次，然后进入“系统设置”→“隐私与安全性”→“安全性”，在 SMS Success Monitor 提示旁点击“仍要打开”，输入本机登录密码并再次确认“打开”。正式消除此提示需要配置 Apple Developer ID 证书和 Apple 公证凭据。
