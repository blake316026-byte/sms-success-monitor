# v0.3.70

- macOS v0.3.70 build 77，Universal 2，支持 Apple Silicon 和 Intel。
- Windows 本次继续提供 v0.3.68，支持 64 位 Windows 10/11。
- Android 本次继续提供已验证的 v0.3.61。
- 修复后台网页已经登录并停留在业务页面，但客户端原生状态仍显示“需登录”并停止扫描的问题。
- 恢复前只读取当前网页会话；仅在没有注销标记、账号唯一且存在页面 Token 时恢复监控；如存在已保存账号，还会要求页面账号与其完全一致。
- 真实注销、账号冲突、账号不一致、无 Token 或旧 Token 仍保持阻断，不改变短信、财务或权限接口。

## 验证

- 完整项目测试、macOS Universal 构建、签名、压缩包完整性和登录状态回归通过。
- 回归覆盖已登录恢复、持久注销标记、内存注销标记、多账号冲突、账号不一致及无 Token。
- 共享 Mac 安装后，`cg07` 仍位于 `/ck-dashboard`，客户端标签由“需登录”恢复为“正常 74.7%”，真实短信扫描恢复。
- GitHub Actions 会重新执行 Windows 共享扫描、财务、权限、会话隔离、打包和运行时自检；Release 附件提供 SHA256 校验清单。

## macOS 首次打开

当前包使用内部临时签名，尚未使用 Apple Developer ID 签名和公证。从 GitHub 下载后若出现“Apple 无法验证”提示，请先将应用放入“应用程序”并尝试打开一次，然后进入“系统设置”→“隐私与安全性”→“安全性”，在 SMS Success Monitor 提示旁点击“仍要打开”，输入本机登录密码并再次确认“打开”。正式消除此提示需要配置 Apple Developer ID 证书和 Apple 公证凭据。
